[project]
name = "flute-scale-detector"
version = "0.1.0"
description = "Flute scale detector application"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

